version: "2"
services:
  workflows:
    image: mf_workflows
    volumes:
      - ./../app:/home/current/app
    command: npm start #top -b #
    environment:
      DEBUG: "true"
      NODE_ENV: development
      APPLICATION_NAME: Workflows
